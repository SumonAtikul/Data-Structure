#include<bits/stdc++.h>
using namespace std;
struct Triangle{
    double base;
    double height;
    double area;
}t[105];
bool compare(Triangle a, Triangle b){
    return a.base<b.base;
}
int main(){
    int n;
    for(int i=0; i<n; i++){
        scanf("%lf %lf", &base, &height);
    }
}
